# inbox

把剛要收進 repo、但還沒正式完成命名與編譯判斷的來源先放這裡。

建議流程：

1. 從 `dedao/快刀廣播站/` 複製 markdown 檔到這裡
2. 補上最小 frontmatter
3. 用 `prompts/markdown-broadcast-ingest.md` 做 ingest judgment
4. 確認值得保留後，再移到 `raw/sources/YYYY/`
